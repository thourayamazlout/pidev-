{% extends 'base2.html.twig' %}

{% block title %}User{% endblock %}

{% block body %}
    <h1>Mon profile</h1>

    <table class="table">
        <tbody>
        
        <tr>
            <th>username</th>
            <td>{{ user.username }}</td>
        </tr>
        <tr>
            <th>Nom</th>
            <td>{{ user.nom }}</td>
        </tr>
        <tr>
            <th>Prenom</th>
            <td>{{ user.prenom }}</td>
        </tr>
        <tr>
            <th>Email</th>
            <td>{{ user.email }}</td>
        </tr>
        <tr>
            <th>Image</th>
            <td><img src="{{ asset('uploads/images/' ~ user.image   ) }}" alt="" style="height: 100px;width: 80px" ></td>
        </tr>


        </tbody>
    </table>



    <a class="btn" href="{{ path('editaccount', {'username': user.username}) }}">edit</a>

    {{ include('user/_delete_form.html.twig') }}
{% endblock %}
