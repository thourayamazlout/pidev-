{% extends 'base2.html.twig' %}
{#
{% block title %}Hello PanierController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>votre liste de favoris ✅</h1>
</div>

    {% if items | length >0 %}
   <div class="table-responsive">
    <tbody class="table">
        <thead>
        <tr>
            <div class="room-caption">
                <th>Nom</th>
                <th>Prix/Dt</th>
                <th>Description</th>
                <th>Ville</th>
                <th>total</th>
            </div>
        </tr>
        </thead>
        {% for item in items %}

        <tr>
            <td>{{ item.escapade.title }}</td>
            <td>{{ item.escapade.price }}</td>
            <td>{{ item.escapade.histoire }}</td>
            <td>{{ item.escapade.ville }}</td>

           {#  <td>{{ item.escapade.prix * item.quantity }}</td>#}
{#
        </tr>
        {% endfor %}
    </tbody>
   </div>
    </table>
            {% else %}
        <p> c'est vide </p>
    {% endif %}

{% endblock %}
#}
{% block body %}
    </br>
    <div class="example-wrapper">
        <h1>votre liste de favoris ✅</h1>
    </div>
<div class="table-responsive">
    <table class="table">
        <thead>
        <tr>
            <div class="room-caption">

                <th>Nom</th>
                <th>Prix/Dt</th>
                <th>Description</th>
                <th>Ville</th>
                <th>aimé</th>
                <th>Total</th>
                <th></th>
            </div>
        </tr>
        </thead>
        <tbody>
    {% for item in items %}
        <tr>

            <td>{{ item.escapade.title }}</td>
            <td>{{ item.escapade.price }}</td>
            <td>{{ item.escapade.histoire }}</td>
            <td>{{ item.escapade.ville }}</td>
            <td>{{ item.quantity }}</td>

            <td>{{ item.escapade.price * item.quantity }}</td>
            <td> <a href="{{ path('supprimer_favoris',{'id_esc': item.escapade.id_esc}) }}" class="badge badge-danger"
                 </td>

        </tr>

</div>
{% else %}
    <tr>
        <td colspan="6">c'est vide</td>
    </tr>
    {% endfor %}

</tbody>
    <tfoot>
    <tr>
        <td colspan="5" class="text-right"> Total: </td>
        <td> {{ total }}</td>
        <td> </td>
    </tr>
    </tfoot>
</br>
</table>
</div>
</form>
{% endblock %}