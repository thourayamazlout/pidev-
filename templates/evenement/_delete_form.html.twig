<form method="post" action="{{ path('evenement_delete', {'idEvent': evenement.idEvent}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ evenement.idEvent) }}">
    <button class=""badge badge-danger">Delete</button>
</form>
