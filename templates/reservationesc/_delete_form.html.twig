<form method="post" action="{{ path('reservationesc_delete', {'ref': reservationesc.ref}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reservationesc.ref) }}">
    <button class="badge badge-danger">Delete</button>
</form>
