<form method="post" action="{{ path('locationv_delete', {'id': locationv.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ locationv.id) }}">
    <button class="badge badge-danger">Delete</button>
</form>
